

export default function resourcePool(stats,level,effects,skills,classMOD) {

    let effectsMod

    for(effect in effects){



    }


  return {

    hp: stats.str * 10 + stats.con * 5,
    mana: stats.int * 12,
    stamina: stats.dex * 8

  };
}